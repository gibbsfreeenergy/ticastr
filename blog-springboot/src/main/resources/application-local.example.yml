# Copy the required values into environment variables or an untracked
# application-local.yml file. Never commit production credentials.
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog?serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: blog
    password: change-me
  mail:
    username: noreply@example.com
    password: change-me

monitoring:
  token: change-me

app:
  redis:
    enabled: false
  search:
    data-root: ./data
    index-path: search-index
  public-api-url: http://localhost:8090

storage:
  active-provider: local
  local-root: ./uploads
  local-public-url: http://localhost:8090/uploads/

website:
  url: http://localhost:8080

qq:
  app-id: replace-me

weibo:
  app-id: replace-me
  app-secret: change-me
